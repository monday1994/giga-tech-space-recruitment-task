import styled from 'styled-components';
import {cardBoxStyle} from '../../assets/styles/base.styles';

export const StyledTogglesListContainer = styled.div`
  ${cardBoxStyle};
  display: flex;
  flex-wrap: wrap;
  padding: 1em;
  margin-top: 1em;
`
